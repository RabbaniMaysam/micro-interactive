# Introductory Microeconomics: Interactive Tools

Source for the class tools site at
**https://rabbanimaysam.github.io/micro-interactive/**

Students pick a chapter, open a tool, and move sliders; the figure redraws
as they move them. Tools are added one at a time as the course proceeds,
so the chapter list is not exhaustive.

## Tools

| Chapter | Anchor | Tool | Inputs (defaults) |
|---|---|---|---|
| 1 | `#budget-line` | Budget line | budget $10 (5 to 15), burger $2 (1 to 4), can of soda $1 (0.5 to 2); axes fixed at 10 burgers by 15 cans |
| 3 | `#demand-experiment` | Demand experiment | five empty rows of price and quantity (0 to 100), typed in class; points plotted and connected in row order |
| 3 | `#demand-move-shift` | Demand: movement along vs. shift | two panels, no card title, each with its own title; the demand line is the segment of P = 105 - Q from Q = 20 to 85, floating inside arrow axes (Plotly's axes hidden, arrows drawn as annotations); left: a vertical price knob (25 to 80) slides a point along the curve and marks P and Q on the axes, no axis titles; right: axes labeled P and Q, a horizontal knob (-25 to 25) draws a blue shifted line beside the black original, an arrow between their midpoints, D1 and D2 at the same height beside the lines' right ends (D2 withheld while the shift is under 7), and "D" with a tight up or down arrow beside the middle of the blue line on the side away from the black one |
| 3 | `#supply-experiment` | Supply experiment | mirror of the demand experiment from ch03 slide 21 (tutoring for 3 hours): same code via a config; both experiments label the columns and axes with the bare words Price and Quantity (no units, no "of 100"; the user's clutter rule, September 9, 2026) |
| 3 | `#supply-move-shift` | Supply: movement along vs. shift | mirror of the demand tool on the segment of P = Q + 5 from Q = 20 to 85; price knob 30 to 85; labels S, S1, S2, and "S" with an arrow placed below and right of the blue line for a rightward shift |
| 3 | `#equilibrium` | Equilibrium, surplus, and shortage | the two segments above cross at Q = 50, P = 55; a vertical price knob (30 to 80, default 55) marks P on the axis; the equilibrium stays marked at every price (gray point, gray dotted guides, "P<sub>e</sub> = 55" in an outer column left of the moving P label and "Q<sub>e</sub> = 50" in a second row under the moving Q labels, so nothing prints over anything; added September 9, 2026); at 55 only those two labels show; away from it two solid points on D and S, dotted guides, Qd and Qs on the axis, and, only while the "Show labels" toggle at the top right is on, Qd and Qs names plus a thick dark red segment between the points labeled "Surplus = n" above 55 or "Shortage = n" below; the toggle is off by default and on every reopen (no spoilers), and with it off both quantities read plainly as "Q = n" |
| 3 | `#shift-equilibrium` | Shifts and the new equilibrium | its own frame (Q 0 to 128, P 0 to 124) with e1 at its center (64, 62) and short D and S segments (Q 35 to 93 before a shift); two horizontal knobs (Shift D, Shift S, each -56 to 56, a tick marking the center) draw the shifted curve in blue as D2 or S2, the new crossing e2, P1, P2, Q1, Q2 on the axes with no numbers, and blue arrows hugging each axis from 1 to 2; a shifted curve's segment is centered on e2 (it slides along its own line as it shifts, so the other curve always cuts it at its midpoint), and an original curve's end is extended when e2 would fall within 8 units of it; a "2" label is offset when it would print on the "1" label |
| 6 | `#elasticity-slope` | Elasticity and slope | two panels titled Demand and Supply, 340px tall, on the DS frame (Q 0 to 115, P 0 to 100) with arrow axes labeled Q and P; each line is a segment of half-length 42 pivoting about (55, 50); a continuous horizontal knob under the figure (step "any") sets the angle from horizontal (0) to vertical (90), default 45; a continuous vertical knob at the left slides the point along the segment (lower end, middle by default, upper end); the point elasticity E = (dQ/dP)(P/Q) prints tight against the point (4px and 1px from its corner) with two decimals and its sign ("E<sub>d</sub> = −0.91"; demand always negative, supply always positive; −∞ or ∞ on a horizontal line, 0.00 on a vertical one); dotted guides read P and Q of the point on the axes, rounded to integers; D or S sits at the right end of the line; the elasticity label is above right (demand) or below right (supply) of the point, switched to the opposite corner when it would leave the frame at the right, print on the line's letter, or sit on the Q axis (added September 10, 2026) |
| 9 | `#long-run-atc` | Short-run ATCs and the long-run ATC | ch09 slides 46 and 47; one 360px figure on a frame of Q 0 to 110 and ATC 0 to 56 with arrow axes labeled Q and ATC; the long-run ATC is LR(Q) = 10 + 0.012 (Q − 50)²; fifteen short-run ATCs exist behind the scenes, ATC<sub>i</sub> being the quadratic tangent to LR at Q = 8 + 6(i − 1) with curvature 0.15 (so LR is their lower envelope), each drawn 9 units either side of its own minimum, which lies right of the tangency on LR's falling side and left of it on the rising side; labels "ATC<sub>i</sub>" sit under each minimum, below the envelope (above the tips the neighbors' tails cross every spot once the family is dense); "−" and "+" buttons under the figure remove the last curve and add the next (ATC<sub>1</sub> alone at the left on open, buttons disabled at 1 and 15); "Dissect" toggle colors each curve blue where it is the lowest of the curves shown and red elsewhere (all black otherwise); "Long-run ATC" toggle draws the thick envelope, labeled at its right end; both toggles off on every reopen; while the envelope is hidden, every crossing of consecutive curves (the output at which the two plants cost the same) is a small dot with dotted guides, its Q on the quantity axis (equal curvatures put the crossing midway between the tangencies: 11, 17, ..., 89) and its cost on the vertical axis rounded to an integer (identical costs of the symmetric pairs print once; labels closer than 13px are spread upward); the "Long-run ATC" toggle hides all of them |
| 9 | `#natural-monopoly` | Natural monopoly | one 360px figure, Q 0 to 22 and vertical axis 0 to 10.5 labeled "Price / Cost per unit" on two lines; a single falling ATC = 1 + 8 e^(−(Q − 1)/4), drawn from Q = 1 (9) to Q = 20 (about 1), labeled "ATC" at its upper-left start; three points on it, A at Q = 4 (4.8), B at Q = 5 (3.9), and PSE&G at Q = 10 (1.8), each with dotted guides, its Q on the quantity axis and its cost (one decimal) on the vertical axis; "Company A" and "Company B" toggles under the figure, both on by default and on every reopen; unchecking one removes that point and adds its output to PSE&G, which slides down the curve (to 14, 15, or 19 with both gone) |
| 10 | `#market-firm` | Market price and the firm's demand | ch10 slide 8; one 360px Plotly figure holding two subplots on the same P scale (the shift-equilibrium frame, Q 0 to 128 and P 0 to 124), titled "Market" (left, 46% of the width) and "Firm" (right); the market panel is the shift-equilibrium geometry (e at (64, 62), D and S segments of half-length 29, each centered on its own shifted position and extended so the crossing stays on both), labeled D and S at the right ends, with "P" on the price axis and "Q" under the crossing, no numbers; the firm panel shows the horizontal line D = P = MR = AR at the market price (labeled below its right end, above it when P < 20), a point on it at q = 60 with a dotted guide and "q" on the axis, and "P" on its own price axis; a dotted price line is a paper-referenced shape, so it runs from the market's P axis through the equilibrium across the gap to the firm's P axis; two knobs under the market panel only (Shift D, Shift S, each -56 to 56 with a center tick) move D and S and the firm's line follows; once a knob leaves the center the original D, S, and firm line stay as thin light-gray ghosts; both knobs at center on every reopen (added September 10, 2026) |
| 10 | `#profit-shutdown` | Profit and the shutdown decision | ch10 slide 31 with one MR line instead of five; one 360px figure, q 0 to 22 and a dollar axis 0 to 18, with MC (teal, dashed below the shutdown point), ATC (red), and AVC (gold) named at their right ends; costs AVC = a − bq + cq² with c = 1/13, b = 20/13, a = 4 + 100/13 (minimum 4 at q = 10), fixed cost 576/13 so that ATC has its minimum 8 at q = 12 and MC passes through both minima; a vertical price knob at the left (2.5 to 14, step 0.1, default 12) is sized and offset so its thumb is level with the MR line; "−" and "+" buttons under the figure step through the analysis, nothing shown at 0 (curves and MR only): 1 the point MC = P with "Q* = n" on the axis; 2 "P = n" and "ATC = n" on the vertical axis (a point on ATC at Q* with a dotted guide) and the remark "Profit is positive because P > ATC" (or "zero because P = ATC", "negative because P < ATC"); 3 "AVC = n" the same way and the remark "Should keep producing because P ≥ AVC" or "Should shut down because P < AVC"; 4 the average profit as a thick vertical segment from ATC to P at Q*, green for a gain and red for a loss, labeled at its right on a white backing; 5 the total profit as the shaded rectangle from 0 to Q* between ATC and P, labeled in its left part (above the line when the band is under 22px tall); remarks and numbers use the same one-decimal rounding, so at the knob's 8.0 the figure reads P = ATC = 8.0 and the remark says zero, and at 4.0 it reads P = AVC = 4.0; vertical-axis labels closer than 14px are pushed apart; remarks sit under the buttons in plain text; step 0 and the default price on every reopen (added September 10, 2026) |
| 10 | `#entry-exit` | Entry, exit, and the long run | ch10 slides 39 to 45 in one figure; the market-and-firm layout (two subplots titled "Market" and "One firm", a dotted price line across the gap) on a shared price scale 0 to 18; market frame Q 0 to 110 with D: P = 12 + 0.25 (23 − Q) and S: P = 12 + 0.25 (Q − 23 − s), so the crossing is Q = 23 + s/2, P = 12 − 0.125 s; S runs from 14 left of its crossing up to P = 17 (long lines as on the slides, named at the top), D is fixed from P = 17 down to Q = 63 (8 right of the farthest crossing; the user's rule, September 10, 2026: D must not stretch or shrink with the knob); the price on the axis (one decimal) and "Q" under the crossing; firm frame q 0 to 22 with the MC and ATC of the profit tool (ATC minimum 8 at q = 12, on MC; ATC drawn to 16.5, named under its right end or above it when the price line passes there), the line D = MR = P at the market price, the point MC = P and the ATC minimum, both with dotted guides and numbers on the axes (prices at the left, quantities below, pushed apart when they would print together); the piece of MC between the two points is thick, green when P is above 8 (profit, entry ahead) and red below (loss, exit ahead), and vanishes at P = 8; one knob "Shift S" under the market panel reading v from 0 to 100 with s = 0.64 v, so P is 12 at the left end, 8 at the center (the tick, the long-run equilibrium), and 4 at the right end, the same distance either side (the user's lopsided-range fix, September 10, 2026); two radio toggles under the knob, "Starting point 1" (v = 0, profit, the default) and "Starting point 2" (v = 100, loss), named neutrally so the toggle does not reveal the answer (the user's rule, September 10, 2026), each putting the knob at its end; once the knob leaves the chosen start, that start's S and firm line stay as gray ghosts and a red arrow at P = 15.5, between the two S lines above D, shows the shift; profit start and knob at 0 on every reopen (added September 10, 2026) |
| 11 | `#demand-mr` | Demand and marginal revenue | ch11 slides 6 to 8 in one figure; frame Q 0 to 22, P 0 to 10, "$7" on the P axis; a horizontal knob "Tilt" (b from 0 to 0.7, continuous, default 0); at 0 one black line P = 7 from the axis to Q = 19 labeled "P = D = AR = MR" at its right end; at b > 0 the black line is D: P = 7 − bQ labeled "P = D = AR" above its right end and a blue line MR: P = 7 − 2bQ labeled "MR" below its right end; both lines end at Q = 19 or where they meet the Q axis, whichever is first (added September 10, 2026) |
| 11 | `#monopoly-profit` | Profit and deadweight loss | ch11 slides 9 to 14; the chapter 10 profit tool's MC (teal) and ATC (red) on a frame Q 0 to 22, dollars 0 to 20, with a linear demand D: P = A − 0.4Q (black, from the axis to Q = 20) and MR = A − 0.8Q (blue, to Q = 20 or the axis); the vertical knob is the intercept A (10 to 18, step 0.1, default 15), its thumb level with the intercept; "−" and "+" step through the analysis from a naked figure: 1 the point MR = MC with "Q* = n" on the axis; 2 the points on D and ATC at Q* with "P = n" and "ATC = n" on the vertical axis (pushed apart when close) and the remark "Profit is positive because P > ATC" (or zero, or negative); 3 the average profit as a thick segment from ATC to P at Q*, green for a gain, red for a loss, labeled at its right on a white backing; 4 the total profit as the shaded rectangle from 0 to Q*, labeled in its left part; 5 the point where D meets MC and the yellow deadweight loss between D and MC from Q* to that point, labeled "DWL = n" (trapezoid-rule area) above and right of that point, pushed up when it would print on the average profit label; no AVC and no shutdown remark, since the slides have neither; step 0 and A = 15 on every reopen (added September 10, 2026) |

Slider ranges: a price runs from half to twice its default; the figure
height is 420px (the user found 600px too tall). The two-panel tool uses
250px-tall panels with a 0.4em gap; the price label on the left panel is
rotated vertical so the left margin stays at 24px.

Each tool is a page of its own: the sidebar link sets the URL hash, the
router shows that `.tool-page` and hides the others, and calls the tool's
redraw function (Plotly cannot size a figure inside a hidden element).
The first tool is shown when the hash is empty or unknown.

## Architecture

One file, `index.html`: inline CSS, inline JavaScript, Plotly from a public
CDN. No build step. Open `index.html` in a browser to preview.

The look (header, sticky chapter nav, sidebar, white tool cards, slider
rail with a reset button) copies the interactive companion site of the
Amazon paper (`RabbaniMaysam/amazon-interactive`).

Adding a tool: add a `<section class="tool-card" id="...">` under the
chapter's heading with a `controls` div and a `plot` div; declare its
slider specs (`label`, `value`, `min`, `max`, `step`, `fmt`) and a draw
function; register it in the `DOMContentLoaded` handler with
`makeControls`. Add the anchor to the sidebar list under its chapter
(chapters are plain labels, tools are links) and, for a new chapter, a
link in the top nav.

The header carries a byline, "By: Maysam Rabbani", under the site title
(added September 9, 2026). The two-panel tools title their panels with the
bare words "Movement along" and "Shift".

House style (the user's, September 8, 2026): no prose on the page beyond
titles. No introductions, descriptions, equations, readouts, tables, or
legends. A tool is one compact slider row and one large figure; labels
live inside the figure.

## Publishing

GitHub Pages serves the `main` branch root. Push to `main` and the site
rebuilds within a minute or two.
